---
import { listArticles } from '../lib/loadContent.ts';
const items = listArticles();
---
<html><body>
    <h1>Verifiable News</h1>
    <ul>
        {items.map(i => <li><a href={`/articles/${i.slug}`}>{i.title}</a></li>)}
    </ul>
</body></html>